const numbers = [38, 94, 444];

const doubled = numbers.map((n) => n * 2);
const addFive = numbers.map((n) => n + 5);
//console.log(addFive);
//console.log(doubled);
//

const friends = ["tom ", "jerrty"];
//const lengths = friends.map((frnd) => frnd.length);
//console.log(lengths);
const firstLetter = friends.map((frns) => frns[0]);
console.log(firstLetter);
